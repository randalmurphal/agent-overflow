// Package theme owns the client-side theme directory
// (<configDir>/themes/): listing theme files as opaque raw JSON,
// reading and atomically writing the appearance selection, and seeding
// the generated schema / TOKENS.md reference at boot.
package theme
