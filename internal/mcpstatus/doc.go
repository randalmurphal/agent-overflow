// Package mcpstatus owns the provider-derived MCP server status cache:
// live-session feeds, on-demand ephemeral fetchers, and the in-memory
// state the bindings expose to the frontend.
package mcpstatus
