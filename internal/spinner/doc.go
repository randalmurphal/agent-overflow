// Package spinner owns the client-side spinner directory
// (<configDir>/spinners/): listing user-dropped sprite strips as an
// opaque (manifest text, PNG bytes) pair and seeding the generated
// SPINNERS.md authoring reference at boot.
package spinner
