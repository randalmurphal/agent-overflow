// Package design implements design mode lifecycle: per-thread working
// directories, file watching, the diagnostic ring buffer, the screenshot
// round-trip broker, the HTTP file handler that serves agent-authored
// pages to the iframe, and the bundled design-mode system prompt.
package design
