// Package workflowhost runs one workflow element — a phase, a fan-out unit, or
// a join — as a live provider turn or a supervised command, reaching the
// process around it only through the Host seam.
package workflowhost
