// Package worktreesetup owns the per-project worktree setup recipe — the files
// copied from the main checkout and the argv commands run in a freshly created
// worktree — plus its validation and its execution engine.
package worktreesetup
