// Run the assistant reveal transition suite in Chromium as well as happy-dom.
// Native Text-node ownership and the terminal direct-DOM handoff are browser
// behavior, so the unit environment is not sufficient evidence on its own.
import './AssistantMessage.streamingReveal.test';
