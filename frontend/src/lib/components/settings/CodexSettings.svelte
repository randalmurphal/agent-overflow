<script lang="ts">
  // The Codex page. See ClaudeSettings.svelte — both are one line over the
  // shared provider page.

  import ProviderSettingsPage from './ProviderSettingsPage.svelte';
</script>

<ProviderSettingsPage provider="codex" />
