diff --git a/dist/Block.svelte b/dist/Block.svelte
index b0a4b6f489414f4ceb7f11d53ab7e420d713958b..707803ff50eed00ed5d22c1f7648dc969f80e6a8 100644
--- a/dist/Block.svelte
+++ b/dist/Block.svelte
@@ -16,7 +16,12 @@
 
 	const streamdown = useStreamdown();
 	const tokens = $derived(
-		lex(isStatic ? block : parseIncompleteMarkdown(block.trim()), streamdown.extensions)
+		lex(
+			isStatic || streamdown.parseIncompleteMarkdown === false
+				? block
+				: parseIncompleteMarkdown(block.trim()),
+			streamdown.extensions
+		)
 	);
 	const insidePopover = getContext('POPOVER');
 </script>
diff --git a/dist/Elements/Code.svelte b/dist/Elements/Code.svelte
index b60d68b7df76d1e580a728f42411d0ac749ab122..7022294de7e730910649dc6f7b9da6b35b7e870d 100644
--- a/dist/Elements/Code.svelte
+++ b/dist/Elements/Code.svelte
@@ -50,7 +50,9 @@
 		const theme = streamdown.shikiTheme;
 		const lang = token.lang;
 		untrack(() => {
-			void highlighter.load(theme, lang);
+			if (highlighter.isReady(theme, lang)) return;
+			const resolveAsync = streamdown.registerAsyncResource?.();
+			highlighter.load(theme, lang).finally(() => resolveAsync?.());
 		});
 	});
 </script>
diff --git a/dist/Elements/Math.svelte b/dist/Elements/Math.svelte
index 5bdcc4a447da9dadf4edf58cbb991030a0c3dc2a..b86dcca4ca37ef1eec370737a5816a9fe0af81da 100644
--- a/dist/Elements/Math.svelte
+++ b/dist/Elements/Math.svelte
@@ -17,9 +17,12 @@
 	let katexInstance = $state<typeof import('katex') | null>(null);
 
 	onMount(() => {
-		import('katex').then((mod) => {
-			katexInstance = mod.default;
-		});
+		const resolveAsync = streamdown.registerAsyncResource?.();
+		import('katex')
+			.then((mod) => {
+				katexInstance = mod.default;
+			})
+			.finally(() => resolveAsync?.());
 	});
 
 	let inner = $state<HTMLElement | null>(null);
diff --git a/dist/Elements/Mermaid.svelte b/dist/Elements/Mermaid.svelte
index 8f6382c3ff28fe730c73a3285f77752fcc540d3a..a2ddc09f0776883de15549737b656ec45706d587 100644
--- a/dist/Elements/Mermaid.svelte
+++ b/dist/Elements/Mermaid.svelte
@@ -20,7 +20,12 @@
 
 	let mermaid = $state<any>(null);
 	onMount(async () => {
-		mermaid = (await import('mermaid')).default;
+		const resolveAsync = streamdown.registerAsyncResource?.();
+		try {
+			mermaid = (await import('mermaid')).default;
+		} finally {
+			resolveAsync?.();
+		}
 	});
 
 	const useIsInsideForMoreThanAQuarterSecond = () => {
diff --git a/dist/Streamdown.svelte b/dist/Streamdown.svelte
index b11c35036bd44f047f19b3b5576b3096ed937874..be81277e1a7622c116c3d707978b70fb2bc228dc 100644
--- a/dist/Streamdown.svelte
+++ b/dist/Streamdown.svelte
@@ -3,6 +3,7 @@
 	import { StreamdownContext, type StreamdownProps } from './context.svelte.js';
 	import { mergeTheme, shadcnTheme } from './theme.js';
 	import { parseBlocks } from './marked/index.js';
+	import { onMount } from 'svelte';
 
 	let {
 		content = '',
@@ -33,6 +34,7 @@
 		mdxComponents,
 		components,
 		static: isStatic,
+		onsettled,
 		...snippets
 	}: StreamdownProps<Source> = $props();
 	import { useDarkMode } from './utils/darkMode.svelte.js';
@@ -155,6 +157,49 @@
 	const id = $props.id();
 
 	const blocks = $derived(isStatic ? content : parseBlocks(content, streamdown.extensions));
+
+	// onsettled fires whenever this Streamdown's async resources have all
+	// resolved. Two firing rules, gated by `armed` (true after one
+	// microtask post-onMount so children's $effects/onMounts have run and
+	// registered any work):
+	//
+	// 1. First arming: if pendingAsyncCount is already 0, fire immediately
+	//    — there was no async work to wait for (plain text, fully-cached
+	//    highlighter / katex / mermaid).
+	// 2. After first fire: re-fire on every transition from >0 to 0.
+	//    Streaming chunks can re-introduce pending work (a new code fence
+	//    arrives mid-stream and needs highlighter.load); consumers can
+	//    debounce on their side if they only care about the first.
+	//
+	// The microtask defer protects against a mount-time race where the
+	// parent's $effect would observe pendingAsyncCount === 0 before child
+	// $effects had run and called registerAsyncResource(). In Svelte 5,
+	// child $effects flush before the parent's, so by the next microtask
+	// after onMount all children have registered.
+	let settledArmed = $state(false);
+	let prevPending = 0;
+	let firstFireDone = false;
+	onMount(() => {
+		queueMicrotask(() => {
+			settledArmed = true;
+		});
+	});
+	$effect(() => {
+		if (!settledArmed) return;
+		const curr = streamdown.pendingAsyncCount;
+		if (!firstFireDone) {
+			if (curr === 0) {
+				firstFireDone = true;
+				onsettled?.();
+			}
+			prevPending = curr;
+			return;
+		}
+		if (prevPending > 0 && curr === 0) {
+			onsettled?.();
+		}
+		prevPending = curr;
+	});
 </script>
 
 <div bind:this={element} class={className}>
diff --git a/dist/context.svelte.d.ts b/dist/context.svelte.d.ts
index d3f64ab18b514401c6515515d09c55825d5dd95c..9e5821a9e77df2d91c31a01cac3805d897b18e6e 100644
--- a/dist/context.svelte.d.ts
+++ b/dist/context.svelte.d.ts
@@ -24,6 +24,8 @@ export declare class StreamdownContext<Source extends Record<string, any> = Reco
         footnotes: Map<string, Footnote>;
     };
     isMounted: boolean;
+    pendingAsyncCount: number;
+    registerAsyncResource(): () => void;
     get animationTextStyle(): string | undefined;
     get animationBlockStyle(): string | undefined;
     constructor(props: Omit<StreamdownProps, keyof Snippets | 'class'> & {
@@ -176,5 +178,6 @@ export type StreamdownProps<Source extends Record<string, any> = Record<string,
             id: string;
         }, any, any>;
     };
+    onsettled?: () => void;
 } & Partial<Snippets<Source>>;
 export {};
diff --git a/dist/context.svelte.js b/dist/context.svelte.js
index 1131b2c3261d104c151289c31e126dea2167ab30..4325191ac0af03ce76ab8c114fb68c1d1ef457d0 100644
--- a/dist/context.svelte.js
+++ b/dist/context.svelte.js
@@ -5,6 +5,22 @@ export class StreamdownContext {
         footnotes: new Map()
     };
     isMounted = false;
+    // Count of async resources currently in-flight: highlighter loads,
+    // katex module import, mermaid module import / render. Each call to
+    // `registerAsyncResource()` increments and returns a one-shot resolve
+    // function that decrements. Reactive so consumers (Streamdown.svelte's
+    // onsettled wiring, embedding apps) can subscribe via $effect.
+    pendingAsyncCount = $state(0);
+    registerAsyncResource() {
+        this.pendingAsyncCount += 1;
+        let done = false;
+        return () => {
+            if (done)
+                return;
+            done = true;
+            this.pendingAsyncCount -= 1;
+        };
+    }
     get animationTextStyle() {
         return getContext('POPOVER')
             ? undefined
diff --git a/dist/marked/index.js b/dist/marked/index.js
index fe8c295baecc3f82c02f7b401325fb32a88514e8..86ece064a98bd7049dcb2339863138e922f1dc23 100644
--- a/dist/marked/index.js
+++ b/dist/marked/index.js
@@ -1,4 +1,8 @@
 import { Lexer } from 'marked';
+// marked 16's GFM del regex treats ~text~ (single tilde) as strikethrough,
+// producing false positives on approximate values like ~240MB. Require ~~.
+const _fixedDel = /^(~~)(?=[^\s~])((?:\\[\s\S]|[^\\])*?(?:\\[\s\S]|[^\s~\\]))\1(?=[^~]|$)/;
+Lexer.rules.inline.gfm.del = Lexer.rules.inline.breaks.del = _fixedDel;
 import { markedAlert } from './marked-alert.js';
 import { markedFootnote } from './marked-footnotes.js';
 import { markedMath } from './marked-math.js';
@@ -48,7 +52,7 @@ export const lex = (markdown, extensions = []) => {
         .lex(markdown)
         .filter((token) => token.type !== 'space' && token.type !== 'footnote');
 };
 export const parseBlocks = (markdown, extensions = []) => {
     const blockLexer = new Lexer(parseExtensions(markedHr, ...markedFootnote(), markedDl, markedTable, markedAlign, markedMdx, ...extensions.filter(({ level, applyInBlockParsing }) => level === 'block' && applyInBlockParsing)));
     return blockLexer.blockTokens(markdown, []).reduce((acc, block) => {
         if (block.type === 'space' || block.type === 'footnote') {
@@ -58,4 +62,4 @@ export const parseBlocks = (markdown, extensions = []) => {
         }
         return acc;
     }, []);
 };
diff --git a/dist/marked/marked-subsup.js b/dist/marked/marked-subsup.js
index 7e63670452888e539d36d95e8580eeba09eb20b2..f823cb14b3791a644a317c66e69c636b0ba8bdee 100644
--- a/dist/marked/marked-subsup.js
+++ b/dist/marked/marked-subsup.js
@@ -1,4 +1,4 @@
-const subRule = /^~([^~\s](?:[^~]*[^~\s])?)~/; // ~text~
+const subRule = /^~([^~\s]+)~/; // ~text~ — no spaces allowed in subscript content
 const supRule = /^\^([^\^\s](?:[^\^]*[^\^\s])?)\^/; // ^text^
 export const markedSub = {
     name: 'sub',
diff --git a/dist/utils/hightlighter.svelte.js b/dist/utils/hightlighter.svelte.js
index 44ad02dae663a9666a57e514f0d235482041b3bd..ff190a15393b475b2bd00e0acbb117a0ea26b9f2 100644
--- a/dist/utils/hightlighter.svelte.js
+++ b/dist/utils/hightlighter.svelte.js
@@ -5,6 +5,11 @@ import { createJavaScriptRegexEngine } from 'shiki/engine/javascript';
 // Import default themes directly
 import githubDark from '@shikijs/themes/github-dark';
 import githubLight from '@shikijs/themes/github-light';
+// Per-line highlight cache — streaming code blocks grow one line at a
+// time; caching each line's tokens avoids full-block re-tokenization.
+const _hlLineCache = new Map();
+const _HL_LINE_CACHE_MAX = 2000;
+const _HL_LINE_CACHE_MAX_AFTER_EVICT = 1500;
 // Default themes that are always loaded
 const DEFAULT_THEMES = {
     'github-dark': githubDark,
@@ -148,45 +153,61 @@ class HighlighterManager {
     /**
      * Highlights code synchronously. Must call isReady() first.
      * Returns plaintext tokens for unsupported languages.
+     *
+     * Uses per-line caching so streaming code blocks (which grow one
+     * line at a time) only tokenize the new/changed last line instead
+     * of re-tokenizing the entire block. Cache key is
+     * `${lang}:${theme}:${lineContent}`. Lines inside multi-line
+     * constructs (block comments, heredocs) may briefly show
+     * single-line tokenization during streaming; the final settled
+     * render produces correct full-block tokens.
      */
     highlightCode(code, language, theme) {
+        const plainLine = (line) => [{ content: line, color: undefined, bgColor: undefined }];
         try {
             const highlighter = this.highlighter;
             if (!highlighter || highlighter instanceof Promise) {
-                // Return plaintext tokens when highlighter is not ready
-                return code.split('\n').map((line) => [
-                    {
-                        content: line,
-                        color: undefined,
-                        bgColor: undefined
-                    }
-                ]);
+                return code.split('\n').map(plainLine);
             }
-            // For unsupported languages, return plaintext tokens
             if (!language || !this.isLanguageSupported(language)) {
-                return code.split('\n').map((line) => [
-                    {
-                        content: line,
-                        color: undefined,
-                        bgColor: undefined
-                    }
-                ]);
+                return code.split('\n').map(plainLine);
             }
-            const tokens = highlighter.codeToTokensBase(code, {
-                lang: language,
-                theme
-            });
-            return tokens;
+            const lines = code.split('\n');
+            const result = new Array(lines.length);
+            let allCached = true;
+            for (let i = 0; i < lines.length; i++) {
+                const key = `${language}:${theme}:${lines[i]}`;
+                const cached = _hlLineCache.get(key);
+                if (cached) {
+                    result[i] = cached;
+                } else {
+                    allCached = false;
+                    result[i] = null;
+                }
+            }
+            if (allCached) return result;
+            // Tokenize uncached lines individually. Cheaper than
+            // full-block tokenization for the common streaming case
+            // where only the last line is new.
+            for (let i = 0; i < lines.length; i++) {
+                if (result[i] !== null) continue;
+                const lineTokens = highlighter.codeToTokensBase(lines[i], {
+                    lang: language,
+                    theme
+                });
+                result[i] = lineTokens[0] ?? plainLine(lines[i]);
+                _hlLineCache.set(`${language}:${theme}:${lines[i]}`, result[i]);
+            }
+            if (_hlLineCache.size > _HL_LINE_CACHE_MAX) {
+                const iter = _hlLineCache.keys();
+                while (_hlLineCache.size > _HL_LINE_CACHE_MAX_AFTER_EVICT) {
+                    _hlLineCache.delete(iter.next().value);
+                }
+            }
+            return result;
         }
         catch (error) {
-            // Return plaintext tokens on error
-            return code.split('\n').map((line) => [
-                {
-                    content: line,
-                    color: undefined,
-                    bgColor: undefined
-                }
-            ]);
+            return code.split('\n').map(plainLine);
         }
     }
     static create(languages = bundledLanguagesInfo, additionalThemes, additionalLanguages) {
diff --git a/dist/utils/parse-incomplete-markdown.js b/dist/utils/parse-incomplete-markdown.js
index 63a979fbe298a9a4a88bfb3d3cd6bba6a3ba1e95..7fb491f4c38f713277d323c75520af15b772fb9c 100644
--- a/dist/utils/parse-incomplete-markdown.js
+++ b/dist/utils/parse-incomplete-markdown.js
@@ -271,6 +271,8 @@ export class IncompleteMarkdownParser {
                     let singleAsterisks = 0;
                     for (let i = 0; i < line.length; i++) {
                         if (line[i] === '*') {
+                            if (isWithinInlineCode(line, i))
+                                continue;
                             const prevChar = i > 0 ? line[i - 1] : '';
                             const nextChar = i < line.length - 1 ? line[i + 1] : '';
                             let lineStartIndex = i;
@@ -298,6 +300,8 @@ export class IncompleteMarkdownParser {
                         let firstSingleAsteriskIndex = -1;
                         for (let i = 0; i < line.length; i++) {
                             if (line[i] === '*' && line[i - 1] !== '*' && line[i + 1] !== '*') {
+                                if (isWithinInlineCode(line, i))
+                                    continue;
                                 const prevChar = i > 0 ? line[i - 1] : '';
                                 const nextChar = i < line.length - 1 ? line[i + 1] : '';
                                 if (/\w/.test(prevChar) && /\w/.test(nextChar))
@@ -364,6 +368,8 @@ export class IncompleteMarkdownParser {
                                 continue;
                             if (isWithinMathBlock(line, i))
                                 continue;
+                            if (isWithinInlineCode(line, i))
+                                continue;
                             if (prevChar &&
                                 nextChar &&
                                 /[\p{L}\p{N}_]/u.test(prevChar) &&
@@ -383,7 +389,8 @@ export class IncompleteMarkdownParser {
                                 line[i - 1] !== '_' &&
                                 line[i + 1] !== '_' &&
                                 line[i - 1] !== '\\' &&
-                                !isWithinMathBlock(line, i)) {
+                                !isWithinMathBlock(line, i) &&
+                                !isWithinInlineCode(line, i)) {
                                 const prevChar = i > 0 ? line[i - 1] : '';
                                 const nextChar = i < line.length - 1 ? line[i + 1] : '';
                                 if (prevChar &&
@@ -792,7 +799,18 @@ export class IncompleteMarkdownParser {
                     return text;
                 }
             }
-        ];
+        ].filter((p) => ![
+            'boldItalic',
+            'bold',
+            'doubleUnderscoreItalic',
+            'strikethrough',
+            'singleAsteriskItalic',
+            'inlineCode',
+            'singleUnderscoreItalic',
+            'subscript',
+            'superscript',
+            'inlineMath',
+        ].includes(p.name));
     }
 }
 // Legacy function for backward compatibility
@@ -833,6 +851,32 @@ const isWithinMathBlock = (text, position) => {
     }
     return inInlineMath || inBlockMath;
 };
+const isWithinInlineCode = (text, position) => {
+    let inSpan = false;
+    let openRunLen = 0;
+    let i = 0;
+    while (i < text.length && i < position) {
+        if (text[i] !== '`') {
+            i++;
+            continue;
+        }
+        let j = i;
+        while (j < text.length && text[j] === '`') {
+            j++;
+        }
+        const runLen = j - i;
+        if (!inSpan) {
+            inSpan = true;
+            openRunLen = runLen;
+        }
+        else if (runLen === openRunLen) {
+            inSpan = false;
+            openRunLen = 0;
+        }
+        i = j;
+    }
+    return inSpan;
+};
 const isWithinFootnoteRef = (text, position) => {
     let openBracketPos = -1;
     let caretPos = -1;
