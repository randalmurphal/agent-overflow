// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Create as $Create } from "@wailsio/runtime";

/**
 * Files is the sounds listing: where the directory is, every playable cue,
 * and every reason a file could not be used.
 * 
 * Warnings are DATA, not log lines. A file that is not a canonical cue is
 * the expected failure here — the user dropped an MP3 in, or an encoder
 * wrote a LIST chunk — and the symptom without a warning is "my sound
 * silently does nothing".
 */
export class Files {
    "dir": string;
    "sounds": Sound[];
    "warnings": string[];

    /** Creates a new Files instance. */
    constructor($$source: Partial<Files> = {}) {
        if (!("dir" in $$source)) {
            this["dir"] = "";
        }
        if (!("sounds" in $$source)) {
            this["sounds"] = [];
        }
        if (!("warnings" in $$source)) {
            this["warnings"] = [];
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new Files instance from a string or object.
     */
    static createFrom($$source: any = {}): Files {
        const $$createField1_0 = $$createType1;
        const $$createField2_0 = $$createType2;
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        if ("sounds" in $$parsedSource) {
            $$parsedSource["sounds"] = $$createField1_0($$parsedSource["sounds"]);
        }
        if ("warnings" in $$parsedSource) {
            $$parsedSource["warnings"] = $$createField2_0($$parsedSource["warnings"]);
        }
        return new Files($$parsedSource as Partial<Files>);
    }
}

/**
 * Sound is one custom cue as it crosses the wire: its id and the validated
 * file, base64-encoded.
 * 
 * WAV is a base64 string rather than a []byte for the mechanical reason
 * spinner.Sprite.PNG is: encoding/json base64-encodes a []byte at runtime,
 * but the Wails binding generator has no special case for it and would emit
 * `number[]` in TypeScript — a declared type that disagrees with the wire.
 * A cue is at most 258 KiB, so it belongs in a frame rather than on the
 * attachment stream.
 */
export class Sound {
    "id": string;
    "wav": string;

    /** Creates a new Sound instance. */
    constructor($$source: Partial<Sound> = {}) {
        if (!("id" in $$source)) {
            this["id"] = "";
        }
        if (!("wav" in $$source)) {
            this["wav"] = "";
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new Sound instance from a string or object.
     */
    static createFrom($$source: any = {}): Sound {
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        return new Sound($$parsedSource as Partial<Sound>);
    }
}

// Private type creation functions
const $$createType0 = Sound.createFrom;
const $$createType1 = $Create.Array($$createType0);
const $$createType2 = $Create.Array($Create.Any);
