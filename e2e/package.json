{
  "name": "agent-overflow-e2e",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:freeze-repro": "playwright test --config=playwright.manual.config.ts freeze-repro.manual.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/node": "24.10.9",
    "typescript": "6.0.2"
  }
}
