# Packet P0.4 — Docs hygiene (root AGENTS.md + design-mode.md)

Milestone M0 of `docs/specs/workflows-system-packets.md`. Read this whole file
before touching anything.

## Standing rules (identical in every M0 packet — obey without exception)

- **No git operations of any kind.** No add/commit/branch/checkout/stash/reset.
  Work in the tree as-is and leave it dirty.
- **Anything not required by the stated intent is out of scope EVEN IF IT WOULD
  HELP.** Do not fix, improve, or tidy adjacent code. If the task appears to
  require work beyond this scope, that is a BLOCKED condition, not permission.
- If a gate or the task seems to require touching a forbidden file, STOP and
  write `BLOCKED.md` at the repo root (include your codex session id and what
  you were trying to do). There is no exception path.
- You may not modify gate commands, test expectations, or any test's
  skip/marker status to make a gate pass. A gate you cannot pass honestly is a
  BLOCKED condition. Paste actual command output (exit code + last ~20 lines)
  into the report for every gate.
- Never edit `frontend/bindings/**` by hand — those files are generated.
- No new dependencies (Go or npm) without a BLOCKED.
- No shims, no mocking the subject under test, never weaken a test to pass it.
- When done, write `PACKET-REPORT.md` at the repo root containing: (1) every
  file touched with a one-line why, (2) an ASSUMPTIONS section listing every
  informed assumption you proceeded on, (3) gate outputs (command, exit code,
  last ~20 lines each), (4) your codex session id.

## Intent

Two documentation corrections, nothing else. This is a docs-only packet: you
read code extensively, you edit exactly two documentation files.

### Part A — root `AGENTS.md` (note: root `CLAUDE.md` is a symlink to it; edit `AGENTS.md` only)

1. **Core Principle 1** currently names one deliberate exception to "Go is
   triage + pipe" (the discussion/deliberation coordination). The workflows
   engine is now in scope (see `docs/specs/workflows-system.md`, status
   Settled) and becomes the second named exception. Rework the principle's
   exception sentence to cover both, in the spirit of:

   > The deliberate exceptions — lightweight coordination when brokering
   > between multiple provider processes and the frontend (deliberation turn
   > tracking, design option flow), and the workflows engine
   > (`internal/workflow/`, spec: `docs/specs/workflows-system.md`), which
   > sequences phases over the same thread/provider runtime — are
   > coordination, not orchestration, and are called out where they live.

   Integrate it so the surrounding prose still reads well; you own the final
   wording, not the meaning.

2. **"Deferred (Not Currently in Scope)" section:** remove the
   "Workflow / phase / gate system" bullet (it is now in active development —
   do not list it as deferred, and do not add an "in progress" tracker here).
   The "Correction-needed / mid-turn correction flow" bullet stays deferred
   but its rationale says "Revisit only if workflows land" — update that
   phrasing: workflows are landing (point at `docs/specs/workflows-system.md`);
   the correction-flow feature itself remains deferred pending its own scope
   conversation.

### Part B — rewrite `docs/architecture/design-mode.md`

The doc is stale — it describes an older implementation. Rewrite it to match
the code as it exists today. Read `internal/design/**`, every `app_design*.go`
(and any design-related frontend surface you need to confirm claims), and
make every sentence in the rewritten doc verifiable against current code.
Keep the structure/tone conventions of the sibling docs in
`docs/architecture/`. Describe what IS: no speculative future content, no
references to the workflows system. Preserve any still-accurate content;
this is a rewrite for accuracy, not for length.

## Out of scope / forbidden

- MUST NOT touch ANY file other than `AGENTS.md` (repo root) and
  `docs/architecture/design-mode.md`. No code edits of any kind. No edits
  under `docs/specs/`.

## Gates

1. `git status --porcelain` shows exactly the two permitted files modified
   (plus your `PACKET-REPORT.md` untracked). Paste the output.
2. Accuracy self-audit for Part B: in the report, include a verification map —
   each substantive claim in the rewritten `design-mode.md` mapped to the
   `file:line` you verified it against. A claim you cannot anchor to code
   must not appear in the doc.

## Report contract

`PACKET-REPORT.md` per the standing rules, plus the verification map (Part B)
and before/after excerpts of the two `AGENTS.md` edits (Part A).
