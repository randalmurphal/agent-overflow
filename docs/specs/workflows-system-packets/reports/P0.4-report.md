# Packet P0.4 Report

Codex session ID: `019f5434-5f98-7ba2-9bea-3b89c2d30656`

## Files Touched

- `AGENTS.md` — named the workflows engine as a coordination exception, removed the obsolete deferred-workflows bullet, and updated the still-deferred correction-flow rationale.
- `docs/architecture/design-mode.md` — replaced the stale migration proposal with a code-anchored description of the current design-mode implementation.
- `PACKET-REPORT.md` — recorded packet scope, assumptions, verification anchors, and gate evidence.

`make install` and the frontend build also materialized ignored prerequisite/output trees (`frontend/node_modules/` and `frontend/dist/`); they do not appear in the packet status gate and contain no authored source changes.

## ASSUMPTIONS

- The packet's explicit `git status --porcelain` gate is the sole exception to its broader “no git operations” rule. No other git command was run.
- “Every file touched” in the report contract means authored, repo-visible changes. Declared dependencies installed into ignored `frontend/node_modules/` and generated build output in ignored `frontend/dist/` are recorded above rather than enumerated file by file.
- Running `make install` after the first gate attempt exposed absent prerequisites was permitted because it installs only dependencies already fixed by the repository lockfile; no dependency declaration or lockfile was changed.
- Current source code and its tests are authoritative for the design-mode rewrite. Historical prose in the old architecture page was retained only where current code supplied a direct anchor.
- Closely related sentences in one paragraph are grouped into a single verification-map row when the same implementation range verifies them.

## Part A — `AGENTS.md` Before/After

### Core Principle 1

Before:

```text
1. **Go is triage + pipe.** No event sourcing, no orchestration engine,
   no in-memory read models. The one deliberate exception — lightweight
   coordination when brokering between multiple provider processes and the
   frontend (deliberation turn tracking, design option flow) — is coordination,
   not orchestration, and is called out where it lives.
```

After:

```text
1. **Go is triage + pipe.** No event sourcing, no orchestration engine,
   no in-memory read models. The deliberate exceptions — lightweight
   coordination when brokering between multiple provider processes and the
   frontend (deliberation turn tracking, design option flow), and the workflows
   engine (`internal/workflow/`; spec: `docs/specs/workflows-system.md`), which
   sequences phases over the same thread/provider runtime — are coordination,
   not orchestration, and are called out where they live.
```

### Deferred section

Before:

```text
- **Workflow / phase / gate system.** Forge has one
  (`apps/server/src/workflow/`); the underlying idea ported from
  `/Users/randy/repos/orc` (see `docs/specs/TASK_TEMPLATES.md`,
  `docs/decisions/ADR-007-human-gates.md`). Not a core feature for v1.
...
- **Correction-needed / mid-turn correction flow.** Forge has this as
  a workflow/gate mechanic (`thread.correct` command, guidance channel
  projection, `correction-needed` interactive-request kind). Both are
  tightly coupled to workflows — which we've also deferred — and
  neither maps to a Codex or Claude wire-level event. t3-code (the
  reference UX we most closely track) doesn't implement either.
  Revisit only if workflows land. If a "let me course-correct
  mid-turn" primitive is wanted independently, it becomes its own
  feature, not forge parity.
```

After:

```text
[The Workflow / phase / gate system bullet is removed.]
...
- **Correction-needed / mid-turn correction flow.** Forge has this as
  a workflow/gate mechanic (`thread.correct` command, guidance channel
  projection, `correction-needed` interactive-request kind). Workflows are
  landing under `docs/specs/workflows-system.md`, but this correction flow
  remains deferred pending its own scope conversation. It does not map to a
  Codex or Claude wire-level event, and t3-code (the
  reference UX we most closely track) doesn't implement either.
  If a "let me course-correct mid-turn" primitive is wanted independently,
  it becomes its own feature, not forge parity.
```

## Part B — Accuracy Verification Map

| Rewritten doc lines | Substantive claim | Verified against |
|---|---|---|
| 3–7 | Design mode uses a per-thread workdir, sandbox preview, chat history, and the named implementation surfaces. | `internal/design/workdir.go:19-84`; `frontend/src/lib/components/design/DesignPreviewPanel.svelte:2-32`; `frontend/src/lib/components/chat/ChatView.svelte:539-561` |
| 11–13 | `design` is selected at creation and cannot be assigned through post-creation mode updates. | `internal/threadmode/threadmode.go:28-45`; `internal/threadmode/threadmode.go:47-74` |
| 15–25 | Session startup ensures the workdir, loads the prompt/override/project context, overrides CWD, then activates watcher/MCP and primes screenshots after prior-session teardown. | `app_design.go:60-107`; `internal/design/prompts.go:217-268`; `app_session.go:75-106`; `app_session.go:125-170` |
| 27–30 | Both providers receive the common prompt and MCP map; Claude uses `--mcp-config`, while Codex writes `mcp_servers` into config overrides. | `app_session.go:350-375`; `internal/provider/claude/session.go:368-381`; `internal/provider/codex/session_helpers.go:30-42` |
| 34–51 | Workdirs are below the DB dir, have `main/` and `options/`, seed `main/index.html`, and no longer retain snapshot/artifact history. | `app_startup.go:404-408`; `internal/design/workdir.go:36-65`; `internal/design/workdir.go:48-54`; `internal/store/migrate_test.go:51-66` |
| 53–58 | Options require `index.html`, picked sets use `.picked`, option bytes remain, and reload hydration selects the latest unpicked set. | `internal/design/workdir.go:87-145`; `internal/design/workdir.go:186-236`; `internal/design/workdir.go:239-315` |
| 60–61 | Path segments are sanitized and thread deletion wipes the design tree. | `internal/design/workdir.go:321-394`; `internal/design/workdir.go:341-350`; `app_thread_delete.go:125-127`; `app_thread_delete.go:165-177` |
| 65–68 | `/design/` is loopback-only; HTML is parsed and prepended with the injected script, while non-HTML streams unchanged. | `internal/transport/server.go:302-341`; `internal/design/server.go:77-102`; `internal/design/server.go:123-140`; `internal/design/server.go:314-356` |
| 70–75 | Preview is an optional companion, uses the cache-busted sandbox URL, offers three exact viewport widths, refresh, and handoff. | `frontend/src/lib/components/chat/ChatHeaderActions.svelte:189-204`; `frontend/src/lib/components/panes/CompanionPane.svelte:27-55`; `frontend/src/lib/components/design/DesignPreviewPanel.svelte:76-80`; `frontend/src/lib/components/design/DesignPreviewPanel.svelte:113-159`; `frontend/src/lib/components/design/DesignPreviewPanel.svelte:261-350`; `frontend/src/lib/types/design.ts:68-77` |
| 77–87 | Recursive watch, debounce, `.tmp` suppression, event mapping, polling fallback, and frontend throttles. | `internal/design/watcher.go:13-33`; `internal/design/watcher.go:86-128`; `internal/design/watcher.go:160-245`; `internal/design/watcher.go:266-303`; `app_design.go:187-208`; `frontend/src/lib/stores/eventsDesign.ts:11-65`; `frontend/src/lib/stores/eventsDesign.ts:75-121` |
| 91–100 | Injected runtime diagnostics flow through source-checked `postMessage` forwarding into a bounded token ring with settle waiting and teardown wakeup. | `internal/design/server.go:374-450`; `frontend/src/lib/components/design/DesignPreviewPanel.svelte:161-234`; `app_design.go:347-377`; `internal/design/diagnostics.go:10-29`; `internal/design/diagnostics.go:76-99`; `internal/design/diagnostics.go:115-176`; `internal/design/diagnostics.go:195-209` |
| 102–115 | MCP starts lazily, uses per-thread URL tokens, exposes exactly two tools, tiles backend Chromium captures to an eight-tile maximum, notes clipping, and returns capture failures as tool errors. | `internal/design/mcp.go:15-97`; `internal/design/mcp.go:169-234`; `internal/design/mcp.go:237-304`; `internal/design/mcp.go:331-354`; `internal/design/reactor.go:54-81`; `internal/design/types.go:88-114`; `app_design.go:25-52` |
| 117–119 | The user handoff has a separate injected, lazy-loaded single-PNG iframe capture path. | `internal/design/server.go:374-395`; `internal/design/server.go:451-535`; `frontend/src/lib/components/design/DesignPreviewPanel.svelte:17-32` |
| 123–127 | Clarification requests are fenced assistant payloads rendered in the chat column; submission sends a normal structured user message. | `internal/design/prompts.go:85-111`; `frontend/src/lib/utils/designAssistantPayload.ts:24-64`; `frontend/src/lib/stores/threadDesignState.svelte.ts:39-56`; `frontend/src/lib/components/chat/ChatView.svelte:554-560`; `frontend/src/lib/components/design/DesignClarificationPicker.svelte:75-107` |
| 129–134 | Options are file/event-driven, replace the main companion preview with sandbox iframes, and send/mark a structured option choice for the next agent turn. | `internal/design/prompts.go:177-201`; `frontend/src/lib/components/design/DesignPreviewRhsPanel.svelte:9-13`; `frontend/src/lib/components/design/DesignOptionsPanel.svelte:49-98`; `frontend/src/lib/components/design/DesignOptionsPanel.svelte:101-190`; `frontend/src/lib/stores/threadDesignState.svelte.ts:59-75` |
| 138–146 | Send-to-thread creates a sibling chat with inherited settings, seeds an unsent path/manifest draft, optionally uploads a best-effort PNG, and rolls back partial creation. | `app_design.go:308-345`; `frontend/src/lib/utils/sendDesignToThread.ts:16-73`; `frontend/src/lib/utils/sendDesignToThread.ts:85-113`; `frontend/src/lib/utils/sendDesignToThread.ts:115-225` |
| 150–158 | One watcher/token registration is owned per active session; teardown and shutdown ordering are explicit; SQLite retains ordinary thread/items while design bytes/markers and UI projections live in their stated layers. | `app_design.go:143-185`; `internal/design/reactor.go:84-93`; `internal/design/mcp.go:99-142`; `app_shutdown.go:187-200`; `app_shutdown.go:252-273`; `internal/design/workdir.go:87-93`; `frontend/src/lib/stores/threadDesignState.svelte.ts:22-37`; `frontend/src/lib/stores/threadDesignState.svelte.ts:39-75` |

Result: PASS. Every substantive claim in `docs/architecture/design-mode.md` has a code anchor above; no unanchored claim remains.

## Prerequisite Verification and Recovery

The tree started clean and the session ID was available. The first gate attempt then showed missing local generated/dependency prerequisites.

### Initial `make go-build`

Exit code: `2`

```text
main.go:33:12: pattern all:frontend/dist: no matching files found
ERROR: no Go packages found
make: *** [Makefile:75: go-build] Error 1
```

### Initial `make go-test`

Exit code: `2`

```text
main.go:33:12: pattern all:frontend/dist: no matching files found
make: *** [Makefile:81: go-test] Error 1
```

### Initial `cd frontend && pnpm run check`

Exit code: `1`

```text
> agent-overflow-frontend@0.0.9 check /home/rmurphy/repos/ao-lanes/p04/frontend
> svelte-check --tsconfig ./tsconfig.json

sh: 1: svelte-check: not found
ELIFECYCLE Command failed.
WARN Local package.json exists, but node_modules missing, did you mean to install?
```

### Initial `cd frontend && pnpm run build`

Exit code: `1`

```text
> agent-overflow-frontend@0.0.9 build /home/rmurphy/repos/ao-lanes/p04/frontend
> vite build --mode production

sh: 1: vite: not found
ELIFECYCLE Command failed.
WARN Local package.json exists, but node_modules missing, did you mean to install?
```

### `make install`

Exit code: `0`

```text
Lockfile is up to date, resolution step is skipped
Packages: +306
Progress: resolved 306, reused 306, downloaded 0, added 306, done
Done in 1s using pnpm v10.33.2
cd frontend && pnpm exec playwright install chromium
```

## Final Repository Gates

### `make go-build`

Exit code: `0`

```text
(no output)
```

### `make go-test`

Exit code: `0`

```text
ok  	agent-overflow/internal/threadmode	0.003s
ok  	agent-overflow/internal/threadtitle	0.005s
ok  	agent-overflow/internal/transport	0.668s
?   	agent-overflow/internal/transport/methodgen	[no test files]
ok  	agent-overflow/internal/triage	22.175s
ok  	agent-overflow/internal/uikeys	0.040s
ok  	agent-overflow/internal/uitrace	0.084s
ok  	agent-overflow/internal/uiwindow	0.045s
ok  	agent-overflow/internal/usagecost	0.003s
ok  	agent-overflow/internal/usermessage	0.006s
ok  	agent-overflow/internal/windowgeom	0.107s
ok  	agent-overflow/internal/workspacefiles	0.215s
ok  	agent-overflow/internal/workspacepath	0.004s
ok  	agent-overflow/internal/wsldistro	0.073s
ok  	agent-overflow/internal/wsllauncher	0.108s
```

### `cd frontend && pnpm run check`

Exit code: `0`

```text
> agent-overflow-frontend@0.0.9 check /home/rmurphy/repos/ao-lanes/p04/frontend
> svelte-check --tsconfig ./tsconfig.json

1783824325547 START "/home/rmurphy/repos/ao-lanes/p04/frontend"
1783824325552 COMPLETED 1548 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
```

### `cd frontend && pnpm run build`

Exit code: `0`

```text
dist/assets/architectureDiagram-ZJ3FMSHR-CT6pjNm1.js          148.91 kB │ gzip:  41.09 kB
dist/assets/javascript-Hk4ZAEKy.js                            174.82 kB │ gzip:  16.62 kB
dist/assets/tsx-BqE6ylDx.js                                   175.60 kB │ gzip:  16.68 kB
dist/assets/jsx-CeeDbO4M.js                                   177.85 kB │ gzip:  16.77 kB
dist/assets/typescript-Cb6HvS8q.js                            181.14 kB │ gzip:  16.29 kB
dist/assets/chunk-WYO6CB5R-Djp-upTk.js                        230.18 kB │ gzip:  36.12 kB
dist/assets/katex-CTs2aUt1.js                                 256.70 kB │ gzip:  76.92 kB
dist/assets/markdown-vendor-B6FJe0KM.js                       387.18 kB │ gzip: 121.39 kB
dist/assets/cytoscape.esm-BkZ_dLQL.js                         435.40 kB │ gzip: 137.92 kB
dist/assets/terminal-vendor-DBbG48j_.js                       465.92 kB │ gzip: 121.55 kB
dist/assets/cpp-DLTJnIL_.js                                   637.55 kB │ gzip:  50.49 kB
dist/assets/chunk-KEIR6QF5-CXN5l0_R.js                        662.57 kB │ gzip: 143.20 kB
dist/assets/index-BAei8bPe.js                               1,090.56 kB │ gzip: 319.93 kB

[plugin builtin:vite-reporter]
(!) Some chunks are larger than 500 kB after minification. Consider:
- Using dynamic import() to code-split the application
- Use build.rolldownOptions.output.codeSplitting to improve chunking
- Adjust chunk size limit for this warning via build.chunkSizeWarningLimit.
✓ built in 3.51s
```

## Packet Gates

### Gate 1 — `git status --porcelain`

Exit code: `0`

```text
 M AGENTS.md
 M docs/architecture/design-mode.md
?? PACKET-REPORT.md
```

### Gate 2 — Accuracy self-audit

Result: PASS. See “Part B — Accuracy Verification Map”; every substantive claim is mapped to a source `file:line` anchor.
