# Packet P3.6 Report

Codex session ID: `019f583e-5348-7251-a18d-d31e8c6b9c2b`

## Result

Packet P3.6 is complete. The untouched baseline and all eight final-tree gates passed. The tree remains dirty; no mutating git operation was used. `BLOCKED.md` was removed after Scope Amendment 1 resolved the picker path mismatch. No generated binding or e2e file was changed.

## File manifest

- `docs/specs/workflows-system-packets/P3.6-sidebar-remote.md` — user-supplied Scope Amendment 1 authorizing the real palette picker paths; not an implementation edit.
- `frontend/src/lib/components/palette/UnifiedThreadPicker.svelte` — excludes workflow-owned thread modes from the global picker.
- `frontend/src/lib/components/palette/UnifiedThreadPicker.test.ts` — covers all three hidden workflow modes in the picker.
- `frontend/src/lib/components/panes/PaneHost.svelte` — renders the real ReviewPane companion for the non-thread workflows source while preserving companion chrome and thread-keyed remounts.
- `frontend/src/lib/components/review/ReviewPane.svelte` — accepts either a normal PanelContext or the explicit workflows review source without freezing reactive workspace metadata.
- `frontend/src/lib/components/sidebar/ProjectItem.svelte` — mounts the per-project workflows section beneath the thread list.
- `frontend/src/lib/components/sidebar/Sidebar.svelte` — mounts the always-present workflows footer immediately above settings.
- `frontend/src/lib/components/sidebar/WorkflowsFooter.svelte` — initializes the independent sidebar projection and renders the global attention entry point.
- `frontend/src/lib/components/sidebar/WorkflowsSection.svelte` — renders per-project run ordering, signals, progress, elapsed state, navigation, and active-run highlight.
- `frontend/src/lib/components/sidebar/__tests__/WorkflowsFooter.test.ts` — covers footer visibility, attention badge, initialization, and overview navigation.
- `frontend/src/lib/components/sidebar/__tests__/WorkflowsSection.test.ts` — covers membership, rollups, ordering, progress degradation, run navigation, and active highlighting.
- `frontend/src/lib/components/sidebar/threadSearch.ts` — excludes hidden modes at the shared project/terminal search predicate.
- `frontend/src/lib/components/sidebar/threadSearch.test.ts` — covers hidden-mode search exclusion.
- `frontend/src/lib/components/workflows/WorkflowActionRow.svelte` — disables every workflow action mutation remotely while leaving navigation live.
- `frontend/src/lib/components/workflows/WorkflowActionRow.test.ts` — covers action-row view-only behavior.
- `frontend/src/lib/components/workflows/WorkflowDetail.svelte` — disables workflow editing and cancellation remotely.
- `frontend/src/lib/components/workflows/WorkflowIntakeDialog.svelte` — guards intake submission/path browsing remotely and refreshes the independent sidebar after enqueue.
- `frontend/src/lib/components/workflows/WorkflowIntakeDialog.test.ts` — covers remote refusal and paused-queue sidebar discovery after enqueue.
- `frontend/src/lib/components/workflows/WorkflowJobNotes.svelte` — makes notes editing local-only and cancels/rechecks debounced saves when locality changes.
- `frontend/src/lib/components/workflows/WorkflowJobNotes.test.ts` — covers the asynchronous local-to-remote debounce race.
- `frontend/src/lib/components/workflows/WorkflowOverview.svelte` — disables queue/studio/triage mutations remotely and refreshes sidebar ordering after successful drag reorder.
- `frontend/src/lib/components/workflows/WorkflowOverview.test.ts` — covers view-only overview controls, live navigation, and sidebar reorder synchronization.
- `frontend/src/lib/components/workflows/WorkflowRunDetail.svelte` — adds the real full-review affordance and disables local-only diff/mutation controls remotely.
- `frontend/src/lib/components/workflows/WorkflowRunDetail.test.ts` — covers full-review routing and view-only run-detail behavior.
- `frontend/src/lib/components/workflows/WorkflowsPane.svelte` — mounts the intake dialog from the existing pane surface.
- `frontend/src/lib/stores/events.ts` — validates notification targets at the wire event boundary.
- `frontend/src/lib/stores/eventsNotification.ts` — binds workflow-item and triage-agent notification routing to real stores/RPCs.
- `frontend/src/lib/stores/eventsNotification.test.ts` — covers notification target parsing at the event boundary.
- `frontend/src/lib/stores/eventsWorkflow.ts` — fans item, phase, and queue events into both pane and always-on sidebar projections.
- `frontend/src/lib/stores/eventsWorkflow.test.ts` — covers sidebar/pane fan-out and queue-triggered refresh.
- `frontend/src/lib/stores/notificationActivationQueue.ts` — validates typed deep links, preserves hydration FIFO, routes workflow targets, and surfaces routing failures.
- `frontend/src/lib/stores/notificationActivationQueue.test.ts` — covers workflow routing, validation/caps, FIFO, dead runs, and sanitized failures.
- `frontend/src/lib/stores/reviewPane.svelte.ts` — records explicit non-thread review targets, supports branch/worktree options, avoids duplicate initial diff loads, and disposes state by expected thread.
- `frontend/src/lib/stores/reviewPane.svelte.test.ts` — covers workflows-source target/state and explicit review options.
- `frontend/src/lib/stores/workflowData.ts` — provides shared pane/sidebar loaders, one-pass project grouping, membership reducers, and sidebar ordering.
- `frontend/src/lib/stores/workflowData.test.ts` — covers shared loading, definition-fetch scope, membership, ordering, and reducers.
- `frontend/src/lib/stores/workflowsPane.svelte.ts` — reuses the shared loader, exposes active-run state, and closes review companions when the workflows source changes.
- `frontend/src/lib/stores/workflowsSidebar.svelte.ts` — owns the indexed, always-on run/definition/phase projection with coalesced authoritative refresh and initialization-race recovery.
- `frontend/src/lib/stores/workflowsSidebar.svelte.test.ts` — covers initialization, retry, event races, queue hydration, omitted summary fields, phase progress, membership, and attention counts.
- `frontend/src/lib/transport/runMode.ts` — adds the reactive bootstrap-derived `isViewOnlySession()` signal.
- `frontend/src/lib/transport/runMode.test.ts` — covers injected, fetched, invalid, and reset locality values.
- `frontend/src/lib/transport/wsClient.ts` — accepts/normalizes bootstrap `remote` and publishes it after manifest validation.
- `frontend/src/lib/transport/wsClient.test.ts` — covers plain-browser and injected bootstrap locality propagation.
- `frontend/src/lib/types/models.ts` — widens `Thread.mode` to the workflow-owned modes.
- `frontend/src/lib/utils/sidebarTree.ts` — excludes hidden modes at the normal sidebar tree construction boundary.
- `frontend/src/lib/utils/sidebarTree.test.ts` — covers hidden roots and children in sidebar trees.
- `frontend/src/lib/utils/threadModes.ts` — centralizes the frontend mirror of `internal/threadmode.hiddenModes`.
- `frontend/src/lib/utils/threadModes.test.ts` — covers the exact hidden-mode set and visible modes.
- `internal/clientmode/clientmode.go` — injects upstream locality for `--connect`, using numeric loopback semantics matching transport authorization.
- `internal/clientmode/clientmode_test.go` — covers non-loopback, standard loopback, full IPv4 loopback-range, and injected bootstrap output.
- `internal/transport/internalmethods.go` — moves exactly the five permitted workflow reads out of LocalOnly and documents the workflow read/mutate split.
- `internal/transport/methods_gen_test.go` — adds exactly the same five reads to `wireSafeMethods`.
- `internal/transport/server.go` — adds per-request `Bootstrap.Remote` using the enforcement locality predicate.
- `internal/transport/server_test.go` — covers loopback/non-loopback bootstrap locality and dispatcher authorization for all five reads plus a mutation refusal.
- `PACKET-REPORT.md` — this completion report.

## ASSUMPTIONS

- Scope Amendment 1 is authoritative and permits exactly the real `components/palette/UnifiedThreadPicker.svelte` file and its test; no other palette file was touched.
- The packet's `frontend/src/lib/components/sidebar/` area includes the shared `threadSearch.ts` predicate and test used by the real project/terminal search boundary.
- A missing/false `remote` bootstrap field means local workflow controls; only the literal boolean `true` enables view-only mode.
- For `--connect`, `localhost`, `::1`, and every numeric IPv4 `127/8` endpoint are local; arbitrary DNS names and non-loopback IPs are remote. The backend remains authoritative and refuses LocalOnly RPCs independently of UI state.
- Sidebar “listed runs” means the decision-2 membership set: not cancelled and not done with a non-empty disposition. Projects containing only resolved/cancelled runs do not require definition catalogs or a section.
- Running phase progress is session-event-only by design; after reload the row shows elapsed-only until a new phase event arrives.
- The newest phase with a thread ID is the review source, matching the existing diff-evidence selection rule.
- The pre-existing Vite chunk-size warning is informational; the build exits successfully and the packet adds no dependency or gate change.
- No binding regeneration was needed because no bound method signature changed.

## §6.3 leak-path verification

- Backend discovery was already covered: `internal/store/threads.go` excludes hidden modes from listing queries.
- Live `thread:updated` was traced through `applyThreadUpdated`/`syncThreadRow` to `syncThread` and the registry's replace-only `replaceThread`; an unknown workflow thread is not inserted into normal discovery by that event. A directly opened workflow thread remains valid in its pane, as required.
- The actual client discovery risks were consumers of any already-cached row: `UnifiedThreadPicker`, the shared project/standalone-terminal search grouping, and sidebar tree construction. These now all use the single `isHiddenThreadMode(mode)` predicate.
- The helper mirrors exactly `workflow`, `workflow-studio`, and `workflow-triage`; it is mode-based, never title-based, and does not prevent direct opening from workflow surfaces.

## Review companion integration

The implementation reuses the real ReviewPane and existing companion registry. `reviewPane.svelte.ts` now records an explicit `{threadId, thread, workspacePath}` target for a source pane that is not a ThreadPane and can defer its first load until the requested branch scope/base is applied. `PaneHost.svelte` handles only the workflows-source review branch, preserves standard review companion chrome/attributes, and keys ReviewPane by phase thread ID. `ReviewPane.svelte` accepts the existing PanelContext or that explicit source. Navigation, sweep movement, retarget, and source deactivation close the workflows companion so a review cannot remain attached to an old run. No parallel diff/review surface was created.

## Peer-locality predicate

`remoteAddrIsLoopback` is the shared backend predicate used by both `handleBootstrap` and the WebSocket upgrade path that supplies `ResolveForOrigin`/LocalOnly enforcement. The bootstrap flag and authorization therefore cannot disagree for a peer. Clientmode cannot observe the kernel peer address, so its private `isLoopbackEndpointHost` applies the same `netip.Addr.IsLoopback` numeric semantics (plus `localhost`) to the configured upstream URL.

## Event payload vs wire-safe reads

Verified: remote-visible workflow events add no privileged data class beyond the newly safe read surface. Item/phase events contain IDs, state/reason, phase/attempt/status already present in item summaries/detail; queue active/concurrency values are already wire-readable settings and remaining-start capacity is compact scheduler status; spend/timing are aggregate cost/detail data; artifact output names are item/artifact metadata. `workflow:error` exports only sanitized user-facing prose and aggregates—the private Go `detail` cause is unexported. Events contain no credentials, raw provider causes, attachment bytes, seeds/snapshots, filesystem contents, or thread-creation capability. `notification:activated` remains loopback-only and `internal/transport/event_visibility.go` is untouched.

## Post-task review

Six read-only review lenses completed. Validated findings were fixed: indexed sidebar projection work, duplicate render computations, hydration and event races, authoritative timestamp/order refreshes, paused-intake/reorder synchronization, intake/job-note async view-only races, review lifecycle/reactivity/chrome, bootstrap comments/tests, and typed notification targets. The security lens found no remaining issue.

## Baseline gates (untouched tree)

### 1. `make go-build`

Exit code: `0`

Tail: `(no output)`

### 2. `make go-test`

Exit code: `0`

```text
ok  agent-overflow/internal/threadmode 0.004s
ok  agent-overflow/internal/threadtitle 0.004s
ok  agent-overflow/internal/transport 0.683s
?   agent-overflow/internal/transport/methodgen [no test files]
ok  agent-overflow/internal/triage 21.434s
ok  agent-overflow/internal/uikeys 0.033s
ok  agent-overflow/internal/uitrace 0.078s
ok  agent-overflow/internal/uiwindow 0.033s
ok  agent-overflow/internal/usagecost 0.003s
ok  agent-overflow/internal/usermessage 0.006s
ok  agent-overflow/internal/windowgeom 0.104s
ok  agent-overflow/internal/workflow/def 0.071s
ok  agent-overflow/internal/workflow/engine 7.577s
ok  agent-overflow/internal/workflow/profile 0.018s
ok  agent-overflow/internal/workflow/runner 0.006s
ok  agent-overflow/internal/workflow/starters 0.007s
ok  agent-overflow/internal/workspacefiles 0.191s
ok  agent-overflow/internal/workspacepath 0.002s
ok  agent-overflow/internal/wsldistro 0.070s
ok  agent-overflow/internal/wsllauncher 0.115s
```

### 3. `go test -race -count=1 ./internal/workflow/... ./internal/threadmode/ ./internal/store/ ./internal/git/ ./internal/notify/ ./internal/transport/`

Exit code: `0`

```text
ok  agent-overflow/internal/workflow/def 1.308s
ok  agent-overflow/internal/workflow/engine 73.271s
ok  agent-overflow/internal/workflow/profile 1.078s
ok  agent-overflow/internal/workflow/runner 1.019s
ok  agent-overflow/internal/workflow/starters 1.040s
ok  agent-overflow/internal/threadmode 1.016s
ok  agent-overflow/internal/store 478.178s
ok  agent-overflow/internal/git 7.111s
ok  agent-overflow/internal/notify 1.013s
ok  agent-overflow/internal/transport 1.605s
```

### 4. `GOOS=windows go build ./cmd/agent-overflow-windows ./...`

Exit code: `0`

Tail: `(no output)`

### 5. `cd frontend && pnpm run check`

Exit code: `0`

```text
> agent-overflow-frontend@0.0.9 check /home/rmurphy/repos/ao-lanes/p36/frontend
> svelte-check --tsconfig ./tsconfig.json

1783892597723 START "/home/rmurphy/repos/ao-lanes/p36/frontend"
1783892597728 COMPLETED 1590 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
```

### 6. `cd frontend && pnpm test`

Exit code: `0`

```text
> vitest run --project unit

RUN  v4.1.4 /home/rmurphy/repos/ao-lanes/p36/frontend

Test Files  379 passed (379)
Tests       5336 passed (5336)
Duration    32.69s
```

### 7. `cd frontend && pnpm run build`

Exit code: `0`

```text
dist/assets/index-DIj0zNKg.js  1,158.96 kB │ gzip: 338.96 kB

[plugin builtin:vite-reporter]
(!) Some chunks are larger than 500 kB after minification. Consider:
- Using dynamic import() to code-split the application
- Use build.rolldownOptions.output.codeSplitting to improve chunking
- Adjust chunk size limit for this warning via build.chunkSizeWarningLimit.
✓ built in 3.27s
```

### 8. `make e2e`

Exit code: `0`

```text
Running 11 tests using 2 workers
✓ boots headless, serves the real SPA, answers harness RPCs
✓ notification activation opens a seeded thread and none logs a no-op
✓ workflow drain completes two phases through the real queue
✓ human workflow gate parks and approves deterministically
✓ seeded projects and history render in the UI
✓ workflow question answer continues on the same mock session
✓ workflow watchdog parks a stalled mock without a test sleep
✓ workflow cancel interrupts a waitSignal-held mock turn
✓ a live mock turn runs the full pipeline and renders
✓ step-gated scenario advances frame by frame via HarnessMockCommand
✓ reset returns the harness to a blank slate
11 passed (4.7s)
```

## Final-tree gates

### 1. `make go-build`

Exit code: `0`

Tail: `(no output)`

### 2. `make go-test`

Exit code: `0`

```text
ok  agent-overflow/internal/threadmode (cached)
ok  agent-overflow/internal/threadtitle (cached)
ok  agent-overflow/internal/transport 0.487s
?   agent-overflow/internal/transport/methodgen [no test files]
ok  agent-overflow/internal/triage 21.252s
ok  agent-overflow/internal/uikeys (cached)
ok  agent-overflow/internal/uitrace (cached)
ok  agent-overflow/internal/uiwindow (cached)
ok  agent-overflow/internal/usagecost (cached)
ok  agent-overflow/internal/usermessage (cached)
ok  agent-overflow/internal/windowgeom (cached)
ok  agent-overflow/internal/workflow/def 0.071s
ok  agent-overflow/internal/workflow/engine (cached)
ok  agent-overflow/internal/workflow/profile (cached)
ok  agent-overflow/internal/workflow/runner (cached)
ok  agent-overflow/internal/workflow/starters (cached)
ok  agent-overflow/internal/workspacefiles 0.193s
ok  agent-overflow/internal/workspacepath (cached)
ok  agent-overflow/internal/wsldistro (cached)
ok  agent-overflow/internal/wsllauncher 0.121s
```

### 3. `go test -race -count=1 ./internal/workflow/... ./internal/threadmode/ ./internal/store/ ./internal/git/ ./internal/notify/ ./internal/transport/`

Exit code: `0`

```text
ok  agent-overflow/internal/workflow/def 1.374s
ok  agent-overflow/internal/workflow/engine 76.104s
ok  agent-overflow/internal/workflow/profile 1.067s
ok  agent-overflow/internal/workflow/runner 1.019s
ok  agent-overflow/internal/workflow/starters 1.029s
ok  agent-overflow/internal/threadmode 1.016s
ok  agent-overflow/internal/store 486.483s
ok  agent-overflow/internal/git 6.986s
ok  agent-overflow/internal/notify 1.014s
ok  agent-overflow/internal/transport 1.524s
```

### 4. `GOOS=windows go build ./cmd/agent-overflow-windows ./...`

Exit code: `0`

Tail: `(no output)`

### 5. `cd frontend && pnpm run check`

Exit code: `0`

```text
> agent-overflow-frontend@0.0.9 check /home/rmurphy/repos/ao-lanes/p36/frontend
> svelte-check --tsconfig ./tsconfig.json

1783895962736 START "/home/rmurphy/repos/ao-lanes/p36/frontend"
1783895962742 COMPLETED 1602 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
```

### 6. `cd frontend && pnpm test`

Exit code: `0`

```text
> agent-overflow-frontend@0.0.9 test /home/rmurphy/repos/ao-lanes/p36/frontend
> vitest run --project unit

RUN  v4.1.4 /home/rmurphy/repos/ao-lanes/p36/frontend

Test Files  386 passed (386)
Tests       5384 passed (5384)
Duration    33.84s
```

### 7. `cd frontend && pnpm run build`

Exit code: `0`

```text
dist/assets/index-CWxO58nY.js  1,173.40 kB │ gzip: 342.77 kB

[plugin builtin:vite-reporter]
(!) Some chunks are larger than 500 kB after minification. Consider:
- Using dynamic import() to code-split the application
- Use build.rolldownOptions.output.codeSplitting to improve chunking
- Adjust chunk size limit for this warning via build.chunkSizeWarningLimit.
✓ built in 3.37s
```

### 8. `make e2e`

Exit code: `0`

```text
Running 11 tests using 2 workers
✓ boots headless, serves the real SPA, answers harness RPCs (481ms)
✓ notification activation opens a seeded thread and none logs a no-op (658ms)
✓ workflow drain completes two phases through the real queue (165ms)
✓ human workflow gate parks and approves deterministically (208ms)
✓ seeded projects and history render in the UI (618ms)
✓ workflow question answer continues on the same mock session (159ms)
✓ workflow watchdog parks a stalled mock without a test sleep (180ms)
✓ workflow cancel interrupts a waitSignal-held mock turn (104ms)
✓ a live mock turn runs the full pipeline and renders (1.4s)
✓ step-gated scenario advances frame by frame via HarnessMockCommand (880ms)
✓ reset returns the harness to a blank slate (685ms)
11 passed (4.8s)
```

---

## Review addendum (Claude, adjudicator)

Full line-level review of the WIP diff (55 files, ~2,200 insertions) split
into five areas: Go/transport, transport-utils frontend, stores, sidebar
components, workflows/review components. **Verdict: not gamed. Zero riders.**

Verified specifically:

- **Exactly-five reclassification.** The five workflow reads moved from
  `LocalOnlyMethods` to `wireSafeMethods` match the packet list;
  `GetBranchBaseDiff` stays LocalOnly. `remoteAddrIsLoopback` is the single
  shared predicate behind both the `Bootstrap.Remote` flag and WS
  enforcement, so the flag and the fence cannot disagree.
- **View-only posture.** `isViewOnlySession()` is reactive
  (createSubscriber), strictly `=== true`, and set only from validated
  bootstrap. Every mutating affordance across ActionRow, Overview, Intake,
  RunDetail, and JobNotes is disabled with `title="Local only"`; reads and
  navigation stay live. JobNotes cancels in-flight debounced saves when
  locality flips, guarded at both schedule and fire.
- **Companion wiring identity.** The workflows pane registers with
  `paneId: WORKFLOWS_PANE_ID`, so `closeCompanionsForSource(paneId)` on
  close targets the same source key used by `openReviewCompanion` and
  PaneHost. `deferInitialLoad` is safe because `setScope` ends in an
  unconditional `reload()`.
- **§6.3 leak paths.** Shared `isHiddenThreadMode` applied at sidebar tree,
  thread search, and the palette picker; `thread:updated` handling is
  replace-only so hidden-mode threads cannot be inserted via events.
- **Gaming sweeps.** No curly quotes/em-dashes in added Go, no
  variables/snapshot/envelope/gateTrace vocabulary in added copy, no test
  `.skip`/`.only`/`.todo`, no gate-config files touched.

Accepted-as-is notes (not defects): the notification deep-link seeds the
run level's `workflowLabel` from the raw workflow id (self-corrects when
definitions load; avoiding it would cost an extra RPC on a rare path), and
the triage deep-link uses `loadThreadById` after `WorkflowOpenTriageAgent`
returns the thread — one extra read, but it is the canonical
wire-conversion + store-registration path. Sidebar tests live in a
`__tests__/` subdirectory; `primitives/__tests__` is existing precedent.

Independent 8-gate battery re-run by the adjudicator post-squash-merge
(with `./internal/transport/` added to the race gate per this packet's gate
list); results recorded in the campaign ledger.
